# UI App to show bundler (webpack) usage with CommonJS and html script libraries

To run in local give the following commands in cloned root

```
 npm install
 npm run build-ui
 npm run start server
```
